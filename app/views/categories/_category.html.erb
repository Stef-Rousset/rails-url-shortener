<div class="flex justify-between items-center h-fit <%= cycle('bg-beige', 'bg-lightblue') %> border rounded p-4 my-8">
  <div class=" flex flex-col justify-between items-start ">
    <p class="mb-4 font-semibold"><%= category.name%></p>
  </div>
</div>
