<% content_for :meta_description do %>
  <%= t '.meta_description' %>
<% end %>
<section class="categories-container w-10/12 lg:w-1/2 mx-auto mt-12">
  <div class="flex justify-between items-center">
    <h1 class="font-bold text-lg my-4"><%= t '.title' %></h1>
    <%= link_to t('categories.index.new_url_link'), new_category_path, class: "bg-beige border rounded border-beige font-semibold w-fit p-2 hover:bg-darkblue hover:text-white" %>
  </div>
    <%= render @categories %>
</section>

